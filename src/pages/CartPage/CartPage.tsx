// CartPage.js
import { classNames } from '../../lib/classNames.ts';

export const CartPage = () => {

    return (
        <div className={classNames('cart-page', {}, [])}>
            <h1>Страница корзины</h1>

        </div>
    );
};
